import { sayHello } from '@calcpad.studio/fortress';
import { Rectangle } from '@calcpad.studio/structures';

const rect = new Rectangle(10, 20);
console.log(rect.secondMomentOfArea('X-X'));

sayHello();