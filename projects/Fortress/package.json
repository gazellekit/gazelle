{
  "name": "fortress",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "prebuild": "dotnet tool restore",
    "build": "dotnet fable --lang javascript && node esbuild.config.mjs"
  },
  "devDependencies": {
    "esbuild": "0.17.16"
  }
}
